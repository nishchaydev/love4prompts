---
import Layout from '../../components/layout/Layout.astro';
import { PromptEnhancer } from '../../components/tools/PromptEnhancer';
import { FAQSection } from '../../components/seo/FAQSection';
import { ToolsCTA } from '../../components/seo/ToolsCTA';

const faqs = [
  { question: "How does the AI prompt enhancer work?", answer: "Our prompt enhancer analyzes your basic prompt and rewrites it with more detail, structure, and specificity. It adds context, constraints, and formatting that AI models respond to better — resulting in higher-quality outputs." },
  { question: "Is the prompt enhancer free to use?", answer: "Yes! You get 5 free enhancements per day, no signup required. Just type your rough idea and get a production-ready prompt instantly." },
  { question: "Which AI models does it work with?", answer: "The enhancer optimizes prompts for ChatGPT, Midjourney, DALL-E, Claude, Gemini, and Flux. Each model has different strengths, and we tailor the enhanced prompt to your chosen target." },
  { question: "Can I enhance image generation prompts?", answer: "Absolutely. Select Midjourney, DALL-E, or Flux as your target model and the enhancer will add visual-specific parameters like lighting, composition, style, and camera angle." },
];

const relatedTools = [

  { label: "Prompt Maker", href: "/tools/prompt-maker" },
  { label: "Image to Prompt", href: "/tools/image-to-prompt" },
];
---

<Layout 
  title="Free AI Prompt Enhancer — Make Any Prompt Better" 
  description="Enhance any basic prompt into a detailed, optimized prompt for ChatGPT, Midjourney, DALL-E, Claude, Gemini, or Flux. Free AI prompt enhancer tool."
 theme="editorial">
  
  <div class="relative w-full min-h-screen overflow-hidden font-['Inter']">
    <div class="relative z-10 container mx-auto max-w-[1200px] border-l border-r border-[#cfc4c5] min-h-[calc(100vh-80px)]">

      {/* Full-width Hero Header */}
      <div class="col-span-1 md:col-span-12 px-6 lg:px-16 pt-8 lg:pt-10 pb-12 border-b border-[#cfc4c5]">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-[#1482A3] text-white rounded-full font-bold tracking-widest text-[12px] uppercase mb-6 shadow-[4px_4px_0_#000] border-2 border-black">
          PROMPT ENHANCER
        </div>
        <h1 class="text-black font-black uppercase tracking-tighter text-5xl md:text-7xl leading-[0.9] mb-6 max-w-5xl">
          Make any prompt better.
        </h1>
        <p class="text-[#4c4546] font-bold text-xl md:text-2xl max-w-3xl leading-tight">
          Enhance any basic prompt into a detailed, optimized prompt for ChatGPT, Midjourney, DALL-E, Claude, Gemini, or Flux.
        </p>
      </div>

      {/* Grid for Tool + Sidebar */}
      <div class="grid grid-cols-1 md:grid-cols-12">
        {/* Left Sidebar */}
        <div class="hidden md:block md:col-span-3 lg:col-span-2 border-r border-[#cfc4c5] relative overflow-visible bg-transparent min-h-[600px]">
          <div class="p-8 sticky top-24 h-full flex flex-col justify-between">
            {/* Rotated TOOLS title reading bottom to top, T at top, centered in sidebar */}
            <h1
              class="absolute top-[64px] left-[20px] xl:left-[50px] text-[180px] lg:text-[240px] leading-[0.75] font-black tracking-tighter text-black uppercase opacity-[0.07] hover:opacity-[0.15] transition-opacity m-0 pointer-events-none z-0"
              style="transform-origin: top left; transform: rotate(-90deg) translate(-100%, 0);"
            >
              TOOLS
            </h1>
          </div>
        </div>

        {/* Right Content Area */}
        <div class="md:col-span-9 lg:col-span-10 p-6 lg:p-16 w-full">
          <PromptEnhancer client:load />
        </div>
      </div>

      {/* Full-width Bottom Area */}
      <div class="px-6 lg:px-16 py-16 lg:py-24 border-t border-[#cfc4c5] flex flex-col gap-24">
        {/* Related Tools */}
        <div class="w-full max-w-6xl mx-auto">
          <p class="text-[14px] font-mono font-bold text-black/50 uppercase tracking-[0.2em] mb-8 text-center">
            Try also
          </p>
          <div class="flex flex-wrap justify-center gap-6">
            {relatedTools.map((tool) => (
              <a
                href={tool.href}
                class="flex-1 min-w-[280px] max-w-[350px] text-center px-8 py-6 rounded-2xl text-xl font-black uppercase tracking-tight border-[3px] border-black bg-white text-black shadow-[6px_6px_0_rgba(0,0,0,1)] hover:shadow-[10px_10px_0_rgba(0,0,0,1)] hover:-translate-y-1 hover:-translate-x-1 hover:bg-[#FF6D87] transition-all no-underline"
              >
                {tool.label} &rarr;
              </a>
            ))}
          </div>
        </div>
        <ToolsCTA />
        <FAQSection client:visible faqs={faqs} />
      </div>
    </div>
  </div>
</Layout>
